image: python:latest

services:
  - mysql:latest

stages:
  - test
  - deploy
  
variables: 
  MYSQL_DATABASE: dev
  MYSQL_ROOT_PASSWORD: dev
  Host: mysql
  User: notroot
  Password: notrootdev
  Database: sushidb

Code quality: 
  stage: test 
  script: 
    - bash ./gitlab_tests.sh
    - python3 ./demo/manage.py test -k
  
Deploy:
  stage: deploy
  script: bash ./gitlab_deploy.sh
  
  
