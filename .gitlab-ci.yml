image: alpine:latest

services:
  - mysql:latest

stages:
  - stage
  - test
  - deploy
  
variables: 
  MYSQL_DATABASE: nigirifalls_db
  MYSQL_ROOT_PASSWORD: root
  DB_HOST: mysql
  DB_USERNAME: root

Code quality: 
  stage: test 
  script: 
    - RUN apk add --no-cache bash
    - bash ./gitlab_tests.sh