image: python:latest

services:
  - mysql:latest

stages:
  - build
  - test

variables: 
  MYSQL_DATABASE: dev
  MYSQL_ROOT_PASSWORD: dev
  Host: mysql
  User: notroot
  Password: notrootdev
  Database: sushidb

Code quality: 
  stage: test 
  script: bash ./gitlabtests.sh

Build:
  stage: build
  script: bash ./gitlabdeploy.sh